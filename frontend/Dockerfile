# указываем на основании чего создается наш образ
FROM node:alpine
# указываем рабочую директорию и автоматически переходим в неё. Далее можно использовать относительный путь
WORKDIR /app
# указываем порт, на котором будет работать наше приложение. Ни на что не влияет, просто для справки
EXPOSE 3000
# копируем файлы package.json и package-lock.json в рабочую директорию, которую указали ранее
COPY package*.json ./
# запускаем команду, которая установит все внешние зависимости
RUN npm install
# копируем все файлы с директории на локальном компьютере(где находится Dockerfile) в образ
COPY . ./
# запускаем наше приложение
CMD [ "npm", "run", "dev" ]